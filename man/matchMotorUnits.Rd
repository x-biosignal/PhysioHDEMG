% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motor-units.R
\name{matchMotorUnits}
\alias{matchMotorUnits}
\title{Match decomposed motor units to a reference decomposition}
\usage{
matchMotorUnits(decomposition, reference, tol = 1L, max_lag = 0L)
}
\arguments{
\item{decomposition}{An \code{hdemg_decomposition} from \code{\link[=hdEMGDecompose]{hdEMGDecompose()}}.}

\item{reference}{The reference decomposition: a list of integer discharge-
sample vectors (one per reference unit), or a \code{PhysioExperiment} carrying
them in metadata \code{reference_mupulses} (e.g. from \code{\link[=readHDEMG]{readHDEMG()}}).}

\item{tol}{Matching tolerance in samples (default 1).}

\item{max_lag}{Maximum constant latency in samples to search when matching
(default 0 = exact alignment; use a few milliseconds' worth for cross-tool
scoring).}
}
\value{
A data frame (class \code{mu_match}), one row per reference unit, with
columns \code{ref_mu}, \code{n_discharges}, \code{matched_unit}, \code{roa_lag_aware},
\code{roa_no_lag}, \code{lag}, \code{pnr_db}, \code{sil}.
}
\description{
Scores a decomposition against a reference set of motor-unit discharge
patterns with a lag-aware rate-of-agreement (RoA): for each reference unit the
best-matching decomposed unit is found (allowing a constant latency of up to
\code{max_lag} samples), and the RoA -- the fraction of discharges the two share
within \code{tol} samples, \code{matched / (n_a + n_b - matched)} -- is reported. This
scores cross-tool \emph{recovery} ("did we recover the units a reference finds, and
which do we miss") rather than bit-identical agreement, so a constant latency
between two decompositions does not penalise a correct match.
}
\references{
Holobar A, Minetto MA, Farina D (2014). Accurate identification of
motor unit discharge patterns from high-density surface EMG and validation
with a novel signal-based performance metric. J Neural Eng 11:016008.
}
\seealso{
\code{\link[=hdEMGDecompose]{hdEMGDecompose()}}, \code{\link[=readHDEMG]{readHDEMG()}}, \code{\link[=muPulseTrains]{muPulseTrains()}}
}
