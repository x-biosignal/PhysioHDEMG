% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motor-units.R
\name{muActionPotentialMaps}
\alias{muActionPotentialMaps}
\title{Motor-unit action-potential maps}
\usage{
muActionPotentialMaps(decomposition, window_ms = 15)
}
\arguments{
\item{decomposition}{An \code{hdemg_decomposition} from \code{\link[=hdEMGDecompose]{hdEMGDecompose()}}.}

\item{window_ms}{Half-window in milliseconds averaged around each discharge
(default 15).}
}
\value{
A length-\code{n_units} list; each element has \code{waveform}
(\verb{(2*w+1) x n_channels} spike-triggered average), \code{amplitude} (per-channel
peak-to-peak), \code{map} (the amplitude reshaped to the \verb{rows x cols} grid, or
\code{NULL} if the grid is unknown), and \code{peak_channel} (the strongest channel).
}
\description{
Spike-triggered average of the raw HD-sEMG for each motor unit, yielding the
multi-channel action-potential waveform and its peak-to-peak amplitude across
the electrode grid (the spatial "map" used to localise the unit).
}
\examples{
sim <- make_hdemg_sim(n_units = 2, duration_sec = 3, grid = c(4, 4),
                      noise_sd = 0.005, seed = 1)
dec <- hdEMGDecompose(sim, n_units = 4, extension_factor = 10, seed = 1)
maps <- muActionPotentialMaps(dec)
maps[[1]]$peak_channel
}
\references{
Farina, D. et al. (2016). Reflections on the evolution of
surface EMG decomposition. J Electromyogr Kinesiol 26:39-46.
}
\seealso{
\code{\link[=hdEMGDecompose]{hdEMGDecompose()}}
}
