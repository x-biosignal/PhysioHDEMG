% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decompose.R
\name{hdEMGDecompose}
\alias{hdEMGDecompose}
\title{Decompose HD-sEMG into motor-unit discharge patterns}
\usage{
hdEMGDecompose(
  x,
  n_units = 8,
  extension_factor = 16,
  sampling_rate = NULL,
  assay_name = NULL,
  refractory_sec = 0.02,
  pnr_threshold = 26,
  sil_threshold = 0.9,
  max_iter = 100,
  tol = 1e-04,
  bandpass = NULL,
  seed = NULL
)
}
\arguments{
\item{x}{HD-sEMG data: an \verb{n_time x n_channels} numeric matrix, an
\code{hdemg_sim} object from \code{\link[=make_hdemg_sim]{make_hdemg_sim()}}, or a \code{PhysioExperiment}
(requires the PhysioCore package).}

\item{n_units}{Maximum number of separation vectors to attempt (default 8).}

\item{extension_factor}{Number of delayed copies per channel used to build
the extended observations (default 16).}

\item{sampling_rate}{Sampling rate in Hz; required when \code{x} is a bare matrix
(taken from the object otherwise).}

\item{assay_name}{Assay to use when \code{x} is a \code{PhysioExperiment}
(default: the first assay).}

\item{refractory_sec}{Minimum inter-discharge interval in seconds used for
peak separation (default 0.02).}

\item{pnr_threshold}{Minimum pulse-to-noise ratio in dB for an accepted unit
(default 26).}

\item{sil_threshold}{Minimum silhouette for an accepted unit (default 0.9).}

\item{max_iter}{Maximum fixed-point iterations per source (default 100).}

\item{tol}{Convergence tolerance on the separation vector (default 1e-4).}

\item{bandpass}{Optional length-2 numeric \code{c(low, high)} in Hz to band-pass the
signal (zero-phase) before decomposition. Recommended for real HD-sEMG
(e.g. \code{c(20, 500)}) to remove baseline drift and high-frequency noise;
\code{NULL} (default) leaves the signal unchanged.}

\item{seed}{Optional integer random seed for reproducibility.}
}
\value{
An \code{hdemg_decomposition} object with \code{pulse_trains} (list of
discharge sample indices), a \code{quality} data frame
(\code{unit}, \code{n_discharges}, \code{discharge_rate}, \code{pnr_db}, \code{sil}), the separation
\code{filters}, and the retained \code{signal}/\code{grid}/\code{sampling_rate} for downstream
firing statistics and action-potential maps.
}
\description{
Decomposes a high-density surface EMG electrode-grid recording into its
constituent motor-unit pulse trains by convolutive blind source separation:
the channels are extended by delayed copies and whitened, each candidate
source is seeded by a fixed-point contrast (kurtosis) maximisation and then
refined by convolution-kernel compensation (the separation vector is
iteratively set to the average whitened observation at the estimated
discharge times), and discharge times are read off by peak detection with a
two-class (spike / noise) split. Sources are accepted on a pulse-to-noise
ratio and silhouette quality gate, and duplicate units are merged.
}
\details{
The default quality gate (\code{pnr_threshold = 26}, \code{sil_threshold = 0.9}) is tuned
for clean data (e.g. \code{\link[=make_hdemg_sim]{make_hdemg_sim()}}). Real HD-sEMG motor units typically
have lower quality (PNR ~20-23 dB, silhouette ~0.85-0.92); for real recordings,
band-pass the signal (\code{bandpass = c(20, 500)}) and relax the thresholds
(\code{pnr_threshold ~ 20}, \code{sil_threshold ~ 0.85}). Validated against openhdemg on
a real 64-channel recording: PhysioHDEMG recovered 4 of 5 reference motor units
at rate-of-agreement 0.79-0.99 (lag-aligned).
}
\examples{
sim <- make_hdemg_sim(n_units = 2, duration_sec = 3, grid = c(4, 4),
                      noise_sd = 0.005, seed = 1)
dec <- hdEMGDecompose(sim, n_units = 4, extension_factor = 10, seed = 1)
dec$quality
}
\references{
Holobar, A. & Zazula, D. (2007). Multichannel blind source
separation using convolution kernel compensation. IEEE TSP 55(9):4487-4496.
Negro, F. et al. (2016). J Neural Eng 13(2):026027.
}
\seealso{
\code{\link[=make_hdemg_sim]{make_hdemg_sim()}}, \code{\link[=muFiringStats]{muFiringStats()}}, \code{\link[=muActionPotentialMaps]{muActionPotentialMaps()}}
}
