% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{hdemgToPhysioExperiment}
\alias{hdemgToPhysioExperiment}
\title{Wrap an HD-sEMG electrode grid in a PhysioExperiment}
\usage{
hdemgToPhysioExperiment(
  signal,
  sampling_rate,
  grid,
  unit = "uV",
  metadata = list()
)
}
\arguments{
\item{signal}{An \verb{n_time x n_channels} matrix of HD-sEMG (channels ordered
column-major over the grid: row varies fastest).}

\item{sampling_rate}{Sampling rate in Hz.}

\item{grid}{Integer \code{c(rows, cols)} electrode-grid layout; \code{rows * cols} must
equal the number of channels.}

\item{unit}{Signal unit stored in the channel metadata (default \code{"uV"}).}

\item{metadata}{Optional named list stored in the object metadata.}
}
\value{
A \code{PhysioExperiment} object.
}
\description{
Builds a \code{PhysioExperiment} (from PhysioCore) holding a single HD-sEMG assay
(\verb{n_time x n_channels}) at the given sampling rate, recording the electrode
grid layout (\code{rows}, \code{cols}) in the channel metadata and object metadata so
that \code{\link[=hdEMGDecompose]{hdEMGDecompose()}} can recover the grid for action-potential maps.
}
\seealso{
\code{\link[=hdEMGDecompose]{hdEMGDecompose()}}, \code{\link[=make_hdemg_sim]{make_hdemg_sim()}}
}
