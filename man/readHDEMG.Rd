% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{readHDEMG}
\alias{readHDEMG}
\title{Read a high-density surface EMG recording from an HDF5 file}
\usage{
readHDEMG(
  path,
  grid = NULL,
  signal_name = "signal",
  rate_name = "sampling_rate",
  unit = "uV"
)
}
\arguments{
\item{path}{Path to the HDF5 file.}

\item{grid}{Optional integer \code{c(rows, cols)} electrode-grid layout override;
taken from the file's \code{grid} dataset when present, otherwise
\code{c(1, n_channels)}.}

\item{signal_name, rate_name}{Dataset names for the signal and the sampling
rate (defaults \code{"signal"} and \code{"sampling_rate"}).}

\item{unit}{Signal unit stored in the channel metadata (default \code{"uV"}).}
}
\value{
A \code{PhysioExperiment} holding the HD-sEMG grid, with any bundled
reference decomposition in metadata \code{reference_mupulses}.
}
\description{
Reads an HD-sEMG electrode-grid recording from an HDF5 (\code{.h5}) file into a
\link[=hdemgToPhysioExperiment]{PhysioExperiment}, the ingest counterpart of
\code{\link[=hdemgToPhysioExperiment]{hdemgToPhysioExperiment()}} for on-disk grids. The file must contain a
\code{signal} dataset (the raw grid signal) and a \code{sampling_rate} scalar; it may
also contain a \code{grid} dataset (\code{c(rows, cols)}) and a \code{reference_mus} group of
per-unit integer discharge-sample vectors -- a bundled reference decomposition
(e.g. exported from the openhdemg sample file) -- which are stored in the
returned object's metadata as \code{reference_mupulses} for scoring a decomposition
with \code{\link[=matchMotorUnits]{matchMotorUnits()}}.
}
\details{
The \code{signal} dataset may be stored \verb{n_time x n_channels} or
\verb{n_channels x n_time}; the longer axis is taken as time.
}
\seealso{
\code{\link[=hdEMGDecompose]{hdEMGDecompose()}}, \code{\link[=matchMotorUnits]{matchMotorUnits()}}, \code{\link[=hdemgToPhysioExperiment]{hdemgToPhysioExperiment()}}
}
