% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{make_hdemg_sim}
\alias{make_hdemg_sim}
\title{Simulate a high-density surface EMG recording with known motor units}
\usage{
make_hdemg_sim(
  n_units = 4,
  duration_sec = 10,
  sampling_rate = 2048,
  grid = c(5, 5),
  firing_rates = NULL,
  muap_length = 24,
  cov_isi = 0.1,
  noise_sd = 0.02,
  seed = NULL
)
}
\arguments{
\item{n_units}{Number of motor units to simulate (default 4).}

\item{duration_sec}{Recording duration in seconds (default 10).}

\item{sampling_rate}{Sampling rate in Hz (default 2048).}

\item{grid}{Integer \code{c(rows, cols)} electrode-grid layout (default
\code{c(5, 5)}); \code{rows * cols} channels are generated.}

\item{firing_rates}{Optional numeric vector of per-unit mean discharge
rates (Hz); defaults to an evenly spaced spread in \verb{[8, 18]} Hz.}

\item{muap_length}{MUAP template length in samples (default 24).}

\item{cov_isi}{Coefficient of variation of the inter-spike interval
(firing-time jitter, default 0.1).}

\item{noise_sd}{Standard deviation of additive white noise, in the same
units as the unit-peak MUAPs (default 0.02; smaller = higher SNR).}

\item{seed}{Optional integer random seed for reproducibility.}
}
\value{
An \code{hdemg_sim} object (a list) with:
\describe{
\item{signal}{An \verb{n_time x n_channels} matrix of simulated HD-sEMG.}
\item{sampling_rate}{Sampling rate in Hz.}
\item{grid}{The \code{c(rows, cols)} grid layout.}
\item{spikes}{A length-\code{n_units} list of integer ground-truth discharge
sample indices.}
\item{discharge_rates}{The realised per-unit mean discharge rate (Hz).}
\item{muap}{A length-\code{n_units} list of \verb{muap_length x n_channels}
ground-truth action-potential templates.}
\item{n_units}{Number of motor units.}
}
}
\description{
Generates a synthetic HD-sEMG electrode-grid signal as the linear
superposition of a known set of motor units, each firing at a target
discharge rate with inter-spike-interval jitter and each with its own
spatial action-potential map over the grid. The ground-truth discharge
times are returned so that a decomposition can be validated (rate of
agreement, discharge-rate error).
}
\examples{
sim <- make_hdemg_sim(n_units = 3, duration_sec = 2, grid = c(4, 4),
                      seed = 1)
dim(sim$signal)
lengths(sim$spikes)
}
\references{
Farina, D. et al. (2010). Decoding the neural drive to muscles
from the surface electromyogram. J Physiol 588(11):1969-1975.
}
\seealso{
\code{\link[=hdEMGDecompose]{hdEMGDecompose()}}
}
